{
  "name": "app",
  "version": "1.0.0",
  "description": "Product Dashboard — Node.js backend",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test src/validators.test.js src/calculators.test.js",
    "lint": "eslint src/",
    "build": "node --check src/server.js && node --check src/validators.js && node --check src/calculators.js && echo 'Syntax OK — build passed'"
  },
  "dependencies": {
    "express": "^5.2.1",
    "lodash": "^4.18.1",
    "pg": "^8.20.0",
    "redis": "^5.12.1"
  },
  "devDependencies": {
    "eslint": "^8.57.1"
  }
}
